import React, { Component } from 'react';
import { Route, Switch, useHistory, useLocation } from 'react-router-dom';

const BoardManager = ({match}) => {
    const menu = match.params.menu;
}

export default BoardManager;
